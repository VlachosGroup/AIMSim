name: Run Tests
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          python -m pip install rdkit-pypi
          python -m pip install networkx==2.1
          python -m pip install openpyxl
          python -m pip install -r requirements.txt
          python -m pip install -e .
          python -m pip install coverage
      
      - name: Run Tests
        run: |
          coverage run --source=. --omit=molSim_ui_main.py,config_reader.py,__init__.py,__main__.py,__setup__.py -m unittest discover
      - name: Show Coverage
        run: |
          coverage report -m
